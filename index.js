/** @format */

import {AppRegistry} from 'react-native';
import MoveList from './pages/MoveList';
import App from './App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => MoveList);
